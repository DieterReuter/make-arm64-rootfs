
machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get install -y tree
  post:
    - docker version
    - docker info

test:
  override:
    - ./build.sh
    - cp rootfs.tar.gz $CIRCLE_ARTIFACTS/
