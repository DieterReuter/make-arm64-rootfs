
machine:
  services:
    - docker

dependencies:
  pre:
    - docker version
    - docker info
  post:
    - docker build -t builder .

test:
  override:
    - docker run --rm -ti builder tar cf - rootfs.tar.gz | tar xvf -
    - mv rootfs.tar.gz $CIRCLE_ARTIFACTS/
