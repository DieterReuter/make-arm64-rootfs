
machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get install -y tree
  override:
    - docker info

test:
  override:
    - echo "No tests."
    - pwd
    - ./build.sh
    - tree
